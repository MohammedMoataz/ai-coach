name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        # 22.5 is the floor the engine declares (node:sqlite); latest catches the next break.
        node: ['22.5', 'lts/*', 'latest']
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - run: node plugins/ai-coach-core/hooks/engine.test.js
      - run: node plugins/ai-coach-core/hooks/hooks.test.js
      - run: node plugins/atlas-coach/tools/ingest.test.js

  manifests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - run: node .github/check-manifests.js
